{
  "compilerOptions": {
    // Layout do Arquivo
    // "rootDir": "./src", // Especifica o diretório raiz dos arquivos de entrada.
    // "outDir": "./dist", // Especifica o diretório de saída para os arquivos compilados.

    // Configurações de Ambiente
    "module": "commonjs", // Especifica o sistema de módulo para o código gerado.
    "esModuleInterop": true, // Permite a interoperabilidade entre módulos CommonJS e ES.
    "target": "esnext", // Especifica a versão do ECMAScript para a qual o código será compilado.
    "types": [], // Lista de pacotes de declaração de tipo a serem incluídos.
    // Para nodejs:
    // "lib": ["esnext"], // Lista de bibliotecas de declaração de tipo a serem incluídas no ambiente de compilação.
    // "types": ["node"], // Inclui tipos de pacotes específicos, como 'node'.
    // e npm install -D @types/node

    // Outras Saídas
    "sourceMap": true, // Gera arquivos .map para depuração.
    "declaration": true, // Gera arquivos .d.ts para módulos TypeScript.
    "declarationMap": true, // Gera arquivos .map para os arquivos .d.ts.

    // Opções de Verificação de Tipo Mais Rigorosas
    "noUncheckedIndexedAccess": true, // Emite um erro para acesso a índices não verificados.
    "exactOptionalPropertyTypes": true, // Garante que propriedades opcionais sejam estritamente 'undefined' se não fornecidas.

    // Opções de Estilo
    // "noImplicitReturns": true, // Emite um erro se nem todos os caminhos de código em uma função retornarem um valor.
    // "noImplicitOverride": true, // Emite um erro quando um método de subclasse sobrescreve um método de classe base sem usar 'override'.
    // "noUnusedLocals": true, // Emite um erro para variáveis locais não utilizadas.
    // "noUnusedParameters": true, // Emite um erro para parâmetros de função não utilizados.
    // "noFallthroughCasesInSwitch": true, // Emite um erro para casos de switch sem 'break'.
    // "noPropertyAccessFromIndexSignature": true, // Emite um erro quando uma propriedade é acessada usando notação de ponto, mas é definida por uma assinatura de índice.

    // Opções Recomendadas
    "strict": true, // Habilita todas as opções de verificação de tipo estritas.
    "jsx": "react-jsx", // Suporte JSX para React.

    "isolatedModules": true, // Garante que cada arquivo possa ser compilado de forma independente.
    "noUncheckedSideEffectImports": true, // Emite um erro para importações que podem ter efeitos colaterais não verificados.
    "moduleDetection": "force", // Força o TypeScript a tratar todos os arquivos como módulos.
    "skipLibCheck": true // Pula a verificação de tipo de todos os arquivos de declaração (.d.ts).
  }
}
